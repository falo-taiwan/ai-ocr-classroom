# AI OCR Classroom Gemini 模仿菜附加說明

## 為什麼要分成三個檔案

這個練習刻意拆成三個對學生可見的檔案：

| 檔案 | 角色 | 是否放完整答案 |
|---|---|---|
| `index.html` | 練習入口，給同學與 Gemini / Canva 看 | 否 |
| `guide.html` | HTML 版附加說明，給人看 | 否 |
| `guide.md` | Markdown 版附加說明，給教材與 AI 上下文使用 | 否 |

這樣可以讓學生比較兩種情況：

1. 只有題目時，AI 會怎麼做。
2. 加入附加說明後，AI 是否更能理解邊界、資料結構與治理要求。

## 練習題目

請做一個單檔 HTML 的教室座位 SVG 工具。

它不是一般座位表，而是一個可以用來教資料結構、座標系、工程規則與 OCR 回灌的教學案例。

## 教室模型

- 老師在前方老師區。
- 學生區有 `3 排 x 2 張長桌`。
- 每張長桌有 `3 個座位`。
- 學生座位總數是 `18`。
- 桌子與座位不可移動。

## 座標系

老師使用圖形座標系：

- 老師卡記錄 `x / y`。
- 老師只能在老師區域內移動。
- 老師不能拖到學生座位。

學生使用座位絕對座標系：

- 學生卡記錄 `seatId`。
- 學生只能吸附學生座位。
- 拖到已有人的座位時，兩位學生交換 `seatId`。

## 人物規則

- 姓名空白時，人物卡不顯示。
- 座位仍然保留。
- 學生可選男 / 女。
- 男生小圓點用藍色。
- 女生小圓點用粉紅色。
- 老師小圓點固定綠色，不受男女欄位影響。

## 範例團體

預設範例團體是英文姓名：

| Name | Gender |
|---|---|
| Alex | male |
| Bella | female |
| Chris | male |
| Diana | female |
| Ethan | male |
| Fiona | female |
| George | male |
| Hannah | female |

範例團體載入密碼是 `20260522`。

這個密碼門檻是靜態頁面的教學級治理示範，不是後端權限控管。

## 資料表設計

匯出 JSON / Excel 時，至少應該能說明這些表：

| Sheet | 用途 |
|---|---|
| people | 人物資料：姓名、角色、性別、色卡、座位或自由座標 |
| seats | 座位資料：固定桌位與座位座標 |
| colors | 色卡資料：OCR 用 RGB 色彩基準 |
| rules | 工程規則：角色邊界、座標系、匯入規則 |
| views | 視角定義：老師視角與學生視角 |

## 工程模式

工程模式不是另一個頁面，也不是右側說明欄。

工程模式應該是：

1. 使用者點一個案件。
2. 系統自動切到學生視角。
3. 在座位 SVG 上浮出註解圖層。
4. 註解用來說明當前規則。

這樣學生可以理解「圖上規則」與「資料規則」是同一件事。

## 限制

練習入口與附加說明不提供完整答案。完整參考實作可由老師另外提供或課後開放。
